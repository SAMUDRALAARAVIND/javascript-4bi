import React from 'react';
import ReactDOM from 'react-dom/client';


const root = ReactDOM.createRoot(document.getElementById("app"));
// const h1 = React.createElement("h1", { id: "test" }, "Basic react app with CRA");

const h1 = (
  <div>
    <h1>dkkdkdk</h1>
  </div>
);

root.render(h1);

// JSX and state